[package]
name = "dogma-vdb-benchmarks"
version = "0.1.0"
edition = "2021"

[dependencies]
dogma-vdb = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
chunker-syntax = ["dogma-vdb/chunker-syntax"]
